{
  "name": "merge-auto-battler",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Merge Battler — mini-jeu web (merge en grille + auto-battler) pour Crazy Games",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
